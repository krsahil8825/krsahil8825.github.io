export const ROUTES = {
    blogs: {
        listingBase: "/blogs/page",
        slugBase: "/blogs",
    },
    projects: {
        listingBase: "/projects/page",
        slugBase: "/projects",
    },
} as const;
